import SwiftUI

enum FilmilaColors {
    static let background = Color(red: 0.07, green: 0.07, blue: 0.10)
    static let surface = Color(red: 0.12, green: 0.12, blue: 0.16)
    static let foregroundPrimary = Color.white
    static let foregroundSecondary = Color.white.opacity(0.68)
    static let danger = Color(red: 0.95, green: 0.35, blue: 0.35)
}
